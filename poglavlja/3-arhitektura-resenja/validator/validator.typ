#import "../../../funkcije.typ": todo, hint

#pagebreak()

== Валидаторски модул
<валидаторски-модул>

Поглављем @архитектуралне-импликације описане су мане при реализацији архитектуре апликације и правила која бивају прекршена. Како наведена правила нису адресирана наивним нити постојећим решењима, јавља се разлог за коришћењем одређене врсте алата за статичку анализу кода. Постојећа решења сама по себи не пружају познат начин одржавања интегритета и валидности датих правила. Библиотека _PoshtaR_ пружа алат за одржавање валидности правила, као и исправљање честих архитектонских грешака које се јављају при развоју апликација које користе неку форму _Mediator_ софтверског обрасца.

Библиотека _PoshtaR_ излаже валидаторски модул, који представља помагало развојном инжењеру. Сврха валидаторског модула је да кршење архитектонских правила пронађе што је раније могуће током развоја апликације. Тиме се развојном инжењеру омогућава рано откривање грешака, чиме се елиминише могућност неисправног рада апликације и спречава отркивање пропуста тек у времену извршавања. Ово условљава да проналазак грешака бива спроведен приликом фазе превођења и изградње апликације, будући да све остале алтернативе могу сакрити грешке и пролонгирати њихов проналазак.

Да би грешке биле пронађене у фази превођења и изградње апликације, валидаторски модул је у сржи реализован као процесор анотација (енг. _Annotation Processor_) @jsr269. Овакво функционисање је подржано путем _Java_ алата за процесирање анотација (енг. _Java Annotations Processing Tool_ - _APT_) @oracle_apt.

Приликом отпочињања превођења и изградње апликације, анотациони процесор спроводи валидацију архитектонских правила. Уколико су сва правила специфицирана од стране процесора валидна, изградња апликације се наставља, при чему се она опционо покреће. Уколико је одређено правило прекршено, неопходно је прекршај правила, сходно његовој озбиљности, пријавити _Java_ преводиоцу @javac. _Java_ преводиоцу се издају два типа сигнала приликом открића прекршаја:

1. *Грешка (енг. _Error_)* - Уколико је тип сигнала сигнал грешке, сигнал се прослеђује _Java_ преводиоцу, те се врши обустава даљег процеса изградње апликације @javac. Специфична грешка (њено име, опис, упутство за њено отклањање) бива исписана на стандардном излазу процеса који је покренуо процес изградње апликације, или унутар визуелне компоненте која представља листу грешака развојног окружења @javac. Овакав приступ се користи за кршење архитектонских правила при чему је могуће ометање целокупног рада апликације, оспоравање рада и сврхе _Mediator_ објекта, или представљају неусаглашеност намере развојног инжењера.

2. *Упозорење (енг. _Warning_)* - Уколико је тип сигнала сигнал упозорења, сигнал се прослеђује _Java_ преводиоцу. Даљи процес изградње апликације при сигналу упозорења се не обуставља @javac. Конкретно упозорење (његово име, опис, упутство за адресирање) бива исписано на стандардном излазу процеса који је покренуо процес изградње апликације @javac. Приступ путем упозорења се користи приликом кршења одређених стандарда који се користе приликом реализације _Mediator_ обрасца, као и поједине неусаглашености намере развојног инжењера.

=== Организација валидаторског модула
<валидаторски-модул-организација>

Валидаторски модул се реализује у два сегмента, те се може визуализовати као:

1. *Сегмент јавног програмског интерфејса* - Овај сегмент садржи анотације које се могу користити у коду апликативне и доменске логике развојног инжењера. Сегмент излаже анотације које служе за управљање валидационим правилима. Служи да омогући њихово заобилажење у случају да је дефинисана логика од стране развојног инжењера суштински исправна, као и да омогући потребе тестирања апликације.

2. *Извршни сегмент* - Овај сегмент садржи сву логику, валидациона правила и ограничења која се примењују приликом процеса превођења и изградње апликације. Примарна улога му је да валидира исправност архитектуре и семантике кода развојног инжењера који примењује парадигме и логику библиотеке _PoshtaR_.


=== Сегмент јавног програмског интерфејса валидаторског модула
<валидаторски-модул-сегмент-јавног-програмског-интерфејса>

Уколико се сматра неопходним, развојни инжењере може да дода овај сегмент уколико је неопходно да релаксира одређена правила. Друга потреба за овим сегментом јесте омогућавање тестирања апликације која користи библиотеку _PoshtaR_, како се при тестирању јавља проблем инјекције компоненти библиотеке. Коришћење _Mediator_ обрасца налаже да се забрани било какав забилазак извршног цевовода, као и спречавање својевољне инјекције и креирања компоненти за које је _Mediator_ објекат надређен. Наведено је адресирано провером да ли је тестна класа означена специфичном анотацијом.

Овај сегмент излаже следеће анотације:

- *`@SuppressDead`* - анотација се користи уколико је логика извршног цевовода исправна и има конкретан завршетак по гранама извршавања, али логика дефинисаног правила сматра супротан случај.

- *`@SuppressOrphan`* - анотација се користи у случајевима да је неопходно дозволити сирочадне дефиниције захтева, иако такве дефиниције немају егзактан обрађивач захтева. 

- *`@OverruleNoInjection`* - анотација се користи за потребе тестирања. Приликом тестирања неопходно је креирати и лажирати понашање компоненти библиотеке. Валидаторски модул забрањује било какво заобилажење позивања _Mediator_ објекта. Ова анотација омогућава да класе означене са тестним анотацијама и при томе се налазе у `test` пакету пројекта инстанцирају тестне дупликате компоненти библиотеке.

Да би користио наведене анотације, развојни инжењер дефинише координате сегмента јавног програмског интерфејса као зависност свог пројекта, односно унутар `<dependencies>` заглавља `pom.xml` датотеке свог пројекта.

=== Извршни сегемент валидаторског модула
<валидаторски-модул-извршни-сегмент>

Да би развојни инжењер користио валидаторски модул као алат за статичку анализу кода, неопходно га је декларисати као процесор анотација унутар конфигурације алата за изградњу пројекта. 

Сам валидаторски модул није потребно укључивати у стандардну листу библиотечких зависности (унутар `<dependencies>` секције `pom.xml` датотеке @mavenapache), из разлога што класе које он садржи служе искључиво за провере током превођења и нису потребне за извршавање саме бизнис и апликативне логике апликације.

У случају коришћења _Apache Maven_ алата, модул се региструје унутар конфигурације `maven-compiler-plugin` прикључка кроз секцију `<annotationProcessorPaths>` @mavenapache @oracle_apt на начин који је приказан листингом @listing-maven-compiler-plugin:

#figure(
  ```xml
  <build>
      <plugins>
          <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-compiler-plugin</artifactId>
              <version>3.13.0</version>
              <configuration>
                  <annotationProcessorPaths>
                      <path>
                          <groupId>io.github.nikola-velemir</groupId>
                          <artifactId>poshtar-validator</artifactId>
                          <version>${project.version}</version>
                      </path>
                  </annotationProcessorPaths>
              </configuration>
          </plugin>
      </plugins>
  </build>
  ```,
  caption: [Конфигурација `maven-compiler-plugin` прикључка за процесирање анотација],
) <listing-maven-compiler-plugin>

#include "pravila-validator.typ"
#include "integracija-komponenti-validator.typ"
#include "dokumentacija-validator.typ"