#import "../../../funkcije.typ": todo, hint

== Документација _Spring_ адаптера
<документација-spring>

Документација _Spring_ адаптера, односно анотације и класе које омогућавају интеграцију са _Spring_ радоим оквиром, описане су табелом @table-spring-documentation.

#figure(
  table(
    columns: (auto, auto, 1fr),
    align: (col, row) => if row == 0 { center + horizon } else { left + top },
    stroke: 0.5pt + luma(150),
    fill: (col, row) => if row == 0 { luma(230) },
    table.header(
      [*Тип*], [*Име*], [*Опис*]
    ),
    [Анотација], [`@EnablePoshtar` #metadata("annotation-enable-poshtar") <анотација-enable-poshtar>], [Служи као примарна улазна тачка и активатор за интеграцију библиотеке _PoshtaR_ у _Spring_ екосистем. Омогућава дефинисање пакета за скенирање компоненти путем атрибута `basePackages`.],
    [Класа], [`PoshtarSpringAutoConfiguration` #metadata("class-poshtar-spring-auto-configuration") <класа-poshtar-spring-auto-configuration>], [Срж адаптера која врши аутоматску конфигурацију инфраструктуре библиотеке. Аутоматски се учитава у апликативни контекст и врши фабрикацију и повезивање кључних компоненти у _Spring_ _Bean_-ове.],
    [Класа], [`PoshtarSpringRegistrar` #metadata("class-poshtar-spring-registrar") <класа-poshtar-spring-registrar>], [Регистратор задужен за програмску регистрацију компоненти унутар апликативног контекста. Филтрира и учитава компоненте означене анотацијама `@Handler` и `@Behaviour` као _Singleton_ _Bean_-ове.],
    [Класа], [`SpringRequestRegistry` #metadata("class-spring-request-registry") <класа-spring-request-registry>], [Имплементација регистра захтева изведена из `AbstractRequestRegistry`. Детектује обрађиваче захтева и преузима кораке цевовода из контекста, користећи `supportsRequest` за проверу генеричких типова и _Proxy_ објеката.],
    [Класа], [`SpringNotificationRegistry` #metadata("class-spring-notification-registry") <класа-spring-notification-registry>], [Конкретна имплементација регистра нотификација изведена из `AbstractNotificationRegistry`. Открива све _Bean_-ове који су обрађивачи нотификација и идентификује њихове генеричке типове ради интеграције у интерну структуру.],
    [Класа], [`SpringPoshtar` #metadata("class-spring-poshtar") <класа-spring-poshtar>], [Конкретна имплементација посредника прилагођена _Spring_ екосистему, изведена из базне класе `PoshtarBase`. Затворена је за даље извођење ради очувања интегритета понашања посредника.]
  ),
  caption: [Документација _Spring_ адаптера библиотеке _PoshtaR_],
) <table-spring-documentation>

// #import "../../../funkcije.typ": todo, hint

// == Документација _Spring_ адаптера
// <документација-spring>

// === Анотације 
// <spring-анотације>

// ==== `@EnablePoshtar`
// <анотација-enable-poshtar>

// Анотација служи као примарна улазна тачка како би се библиотека _PoshtaR_ интегрисала унутар _Spring_ екосистема. Конципирана је по узору на стандардне _Spring_ анотације, које служе као активатори (попут `@EnableAsync` или `@EnableScheduling`), које омогућавају развојним инжењерима да једноставним постављањем анотација на својим апликативним или конфигурационим класама активирају комплетан систем за регистрацију компоненти @spring.

// Овај приступ увођења функционалности путем активаторских анотација је стандардни приступ који развојни инжењери користе приликом развоја апликација путем _Spring_ радног оквира @spring, те библиотека жели да постигне познату парадигму којом се радни оквир служи @искуствене-импликације.

// Путем атрибута `basePackages` омогућава се експлицитно дефинисање пакета пројекта у оквиру којих се врши скенирање у потрази са компонентама библиотеке. Уколико развојни инжењер не наведе конкретне пакете одакле полази скенирање, за коренски пакет се узима пакет оне класе над којом је анотација постављена.

// === Класе 
// <spring-класе>

// ==== `PoshtarSpringAutoConfiguration`
// <класа-poshtar-spring-auto-configuration>

// Класа служи као срж адаптера која врши аутоматску конфигурацију инфраструктуре коју нуди библиотека. Заједно са анотацијом `@EnablePoshtar` омогућава „укључи и користи" искуство приликом коришћења библиотеке. Класа се аутоматски учитава у апликативни контекст и врши аутоматску фабрикацију и повезивања свих кључних компоненти језгра у функционалне _Spring_ _Bean_-ove.

// Свака метода унутар класе омогућава креирање подразумеваних имплементација регистара и конфигурације цевовода како би се омогућило функционисање истог момента када се библиотека интегрише у апликацију. Исто тако развојном инжењеру се оставља могућност да поново дефинише компоненте које су му неопходне на свој начин (нпр. креација засебне конфигурације цевовода класом `PipelineConfiguration`). _Spring_ ће дати предност корисничком коду и заобићи иницијализацију подразумеваних компоненти @spring.

// Путем глобалног објекта који репрезентује апликативни контекст, омогућено је да при покретању апликације креирани _Bean_-ови буду искоришћени ради изградње интерне структуре библиотеке. Неопходне компоненте се екстрахују из конкретног апликативног контекста ради фабрикације свих регистара и _Mediator_ објекта.

// ==== `PoshtarSpringRegistrar`
// <класа-poshtar-spring-registrar>

// Ова класа је регистратор задужен за програмску регистрацију свих компоненти библиотеке унутар апликативног контекста. Ова компонента се активира приликом покретања апликације, омогућавајући да се упишу све потребне дефиниције _Bean_-ова којима управља радни оквир.

// Класа селективно филтрира компоненте, при чему од свих компоненти у апликативном контексту учитавају само оне које су означене анотацијама `@Handler` и `@Behaviour` из језгра. Путања скенирања компоненти је ограничена путем пакета специфицираних путем анотације `@EnablePoshtar`. Све пронађене компоненте се инкорпорирају у апликативни контекст тако што се трансформишу у _Bean_-ове, чији је обим животног циклуса дефинисан као _Singleton_.

// ==== `SpringRequestRegistry`
// <класа-spring-request-registry>

// Ова класа представља конкретну имплементацију регистра захтева прилагођену за _Spring_ радни оквир, изведену из апстрактне класе `AbstractRequestRegistry`. Њена улога је детекција свих _Bean_-ова који представљају обрађиваче захтева, као и преузимање дефинисаног редоследа корака цевовода, како би кроз проверу генеричких ограничења конструисала одговарајуће ланце инвокације за конкретан тип захтева.

// На основу уређене листе добијене из конфигурације цевовода, класа повлачи све дефинисане кораке као инстанце _Bean_-ова садржаних у апликативном контексту. Редефиницијом методе `supportsRequest` омогућено је филтрирање добављених инстанца корака цевовода на основу њихове компатибилности са конкретним типом захтева. Механизми дефинисани унутар ове методе врше резолуцију генеричких типова, са посебним освртом на то да радни оквир може обухватити компоненту у _Proxy_ објекат @spring.

// ==== `SpringNotificationRegistry`
// <класа-spring-notification-registry>

// Ова класа је конкретна имплементација регистра нотификација, изведена из апстрактне класе `AbstractNotificationRegistry`. Њен задатак је да омогући откривање свих _Bean_-ова који су обрађивачи нотификација, те идентификује генеричке типове нотификација који они обрађују како би их интегрисао у интерну меморијску структуру.

// ==== `SpringPoshtar`
// <класа-spring-poshtar>

// Класа представља конкретну имплементацију посредника прилагођену _Spring_ радном оквиру, изведену из базне класе `PoshtarBase`.

// Намерном дизајнерском одлуком се спречава даље извођење класе, чиме се гарантује интегритет и очување дефинисаног понашања посредника унутар контекста.

